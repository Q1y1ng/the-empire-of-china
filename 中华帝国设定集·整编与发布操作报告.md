# 《中华帝国设定集》整编与发布操作报告

> 项目：中华帝国（架空历史）世界观设定
> 版本：继文十一年（2026）
> 报告日期：2026-09-20
> 整编范围：**全部设定文档**——世界观总设定 v2.0／编年史／世界近代史推演／长安城市志／全国官员名录（42 卷＋总索引）
> 排除：《华北风暴》小说正文（按指示不入设定集，单独文件保留）
> 仓库：`https://github.com/Q1y1ng/the-empire-of-china.git`（main）

---

## 0. 摘要

本轮将分散写作的**全部世界观设定文档**整编为单一《中华帝国设定集·全卷合订本》，经五轮全量审查修复后，用本机便携工具链（pandoc + typst）发布**桌面 PDF 设定集**：194 页，正文宋体、标题方正小标宋、自动目录，字体全嵌入。

| 交付物 | 位置 | 规格 |
| --- | --- | --- |
| 核心设定 4 件 | `设定/`（总设定 v2.0／编年史／近代史推演／长安城市志） | 2720＋1106＋867＋399 行 |
| 官员名录 44 件 | `设定/全国官员名录·卷一…卷四十二`、`总索引(2026).md`、合订本 | 约 4300＋ 具名岗位 |
| 名录合订本 | `设定/全国官员名录·全卷合订本(2026).md` | 6158 行 / 342 KB |
| **设定集合订本** | `设定/中华帝国设定集·全卷合订本(2026).md` | **11 263 行 / 585 KB / 47 部分 / 114 个一级标题** |
| **设定集 PDF** | 桌面 `中华帝国设定集(2026).pdf` | **194 页 / 7.0 MB / 字体全嵌入 / 自动目录 3 页** |
| 名录 PDF | 桌面 `全国官员名录·全卷合订本(2026).pdf` | 103 页 / 4.68 MB（分册版，字体规范同设定集） |
| 汇编脚本 | `tools/build_setting_book.py` | 幂等；汇编输出经字节级比对验证可复现 |
| 本报告 | 仓库根目录 | 全过程＋可复现命令＋坑位记录 |

---

## 1. 收录范围与资产结构

### 1.1 收录（47 部分）

| 序 | 文档 | 行数 | 说明 |
| --- | --- | --- | --- |
| 1 | 中华帝国世界观总设定 v2.0 | 2720 | 世界观圣经（冻结版骨架） |
| 2 | 中华帝国编年史（约1360—2026） | 1106 | 编年体正史 |
| 3 | 世界近代史推演（约1360—2026） | 867 | 国际史因果链推演 |
| 4 | 长安城市志 | 399 | 首都专史 |
| 5 | 全国官员名录（总索引＋42 卷） | 6158（合订本口径） | 一品皇帝至吏三级全链名录 |

汇编顺序固定为：总设定 → 编年史 → 近代史推演 → 长安城市志 → 官员名录（总索引在先、卷一至卷四十二顺次），各部分以 `---` 分隔，卷首加"收录范围／符号约定"说明头。

### 1.2 排除

- 《华北风暴·正文·扩写稿.md》（仓库根目录，926 KB）——按指示**不入设定集**。其人物仅作为名录卷四十二"1990 年对照"的设定素材引用。

---

## 2. 整编流程

### 2.1 设定集汇编（脚本化）

汇编由 `tools/build_setting_book.py` 完成（幂等，可重复执行）：

```python
# tools/build_setting_book.py —— 要点
# 1) 4 个核心文档 + 总索引 + 卷一…卷四十二（显式中文数字序收集）
# 2) 各部分 strip 后以 '\n\n---\n\n' 连接，前置说明头
# 3) 输出 设定/中华帝国设定集·全卷合订本(2026).md
# 4) --pdf OUT.pdf 时继续：pandoc(gfm→typst) → 注入字体/版式 → typst compile
```

⚠️ 中文卷序**不能**用 `sorted()`（字典序 ≠ 中文数字序），脚本内为显式列表。

### 2.2 官员名录五轮全量审查（前置）

| 轮次 | 检查项 | 方法 | 结果 |
| --- | --- | --- | --- |
| 1 | 地名黑名单（600＋ 词） | 表格姓名提取 → 黑名单交集 | 0 命中 |
| 2 | 草稿痕迹 | grep（待定／待后续／××／【推演待定】／…） | 4 类残留 → 已修 |
| 3 | 全库人名重名 | 跨 43 文件姓名比对（324 项命中） | 全部定性合法：引用链／藏名放行（卷十九）／机构与表头词 |
| 4 | 引用链闭环 | 39 省府市长 ↔ 城市卷；秘书长/审计署长 ↔ 各署卷；区主席 ↔ 城市卷 | 修复 1 处断裂（卷二十四）后**全闭环** |
| 5 | 品级一致性 | 各卷品级列抽查（六品州府／七品省府／九品区） | 修复 1 处笔误（卷三十二） |

### 2.3 修复记录（6 类）

1. **【重大】卷二十四五市双市长**：太原/济南/郑州/大连/天津第 2 席自造名与卷二十二省府市长冲突（同城两人）→ 改为引用现名 **晋原平／鲁泉生／郑汴生／连海澄／郎津安（卷二十二）**；孤儿名 5 个清除，残留 = 0。
2. 卷三十二：西宁湟水区第 2 席品级 十品 → **九品**（与其余五区一致）。
3. 卷二十七：乌兰巴托应急局长"巴达玛"撞卷二十二 → 改 **阿日善**。
4. 卷二十三删除过时"注【推演待定】"；卷一三大舰队"待后续卷展开"改为已完成索引注（卷二／三十三／三十五）。
5. 31 处过时"下一卷计划" → 统一改"全卷导航见总索引"。
6. 卷四十二："汪明鉴※注※"衍字勘误。

有意保留项：`驻××州` 通配符（非草稿）；卷四十二"昭穆帝"名讳（作者决策点，保留【推演】标记）。

### 2.4 合订本维护规则

- 任何分卷修改后，必须**重拼合订本 → 重编译 PDF**（本轮已固化为脚本次序）。
- 名录合订本头部曾滞后（"卷一至三十二／约 3900＋ 岗位"）——本轮勘误为"**卷一至卷四十二／约 4300＋ 岗位**"；头部信息改由脚本/统一门户维护，避免手写漂移。

---

## 3. PDF 发布管线

### 3.1 选型

| 方案 | 决策 |
| --- | --- |
| LaTeX（xelatex） | 不采用：本机无 TeX 发行版，体积大 |
| wkhtmltopdf | 不采用：不在本机便携工具集 |
| **pandoc 3.10.2 + typst 0.15.1** | **采用**：`E:\AI\tools\pandoc`、`E:\AI\tools\typst`，单二进制、原生中文字体、编译快 |

### 3.2 字体方案

| 用途 | 字体 | 设置 |
| --- | --- | --- |
| 正文 | **宋体 SimSun** | 10.5pt，两端对齐 |
| 标题 H1/H2/H3 | **方正小标宋 FZXiaoBiaoSong-B05S** | 16／13／11.5pt，regular 字重（公文标题风） |
| 表格 | 宋体 | 9pt，框线 0.4pt |
| 页面 | — | 边距 1.8cm×2.0cm；页码；自动目录（depth 1，3 页） |

### 3.3 生成命令

```bash
# 汇编 + 出 PDF（一行完成；pandoc、typst 需在 PATH）
py -3 tools/build_setting_book.py --pdf "$USERPROFILE/Desktop/中华帝国设定集(2026).pdf"
```

脚本内部等价于：

```bash
pandoc -f gfm "设定/中华帝国设定集·全卷合订本(2026).md" -t typst -o "$TEMP/setting_book.tmp.typ"
# python 注入字体/版式/目录（见脚本 TYPST_INJECT 常量）
typst compile "$TEMP/setting_book.tmp.typ" "输出.pdf"
```

注入的 typst 设置（脚本内 `TYPST_INJECT`）：

```typst
#set document(title: "中华帝国设定集·全卷合订本（2026）", author: "Q1y1ng")
#set text(font: ("SimSun", "FZXiaoBiaoSong-B05S", "Microsoft YaHei", "Libertinus Serif"), size: 10.5pt, lang: "zh")
#set par(justify: true)
#show heading.where(level: 1): set text(font: ("FZXiaoBiaoSong-B05S", "SimSun"), size: 16pt, weight: "regular")
#show heading.where(level: 2): set text(font: ("FZXiaoBiaoSong-B05S", "SimSun"), size: 13pt, weight: "regular")
#show heading.where(level: 3): set text(font: ("FZXiaoBiaoSong-B05S", "SimSun"), size: 11.5pt, weight: "regular")
#show heading: set block(above: 1.2em, below: 0.6em)
#set page(margin: (x: 1.8cm, y: 2.0cm), numbering: "1")
#set table(stroke: 0.4pt, inset: (x: 4pt, y: 2.5pt))
#show table: set text(font: ("SimSun", "Microsoft YaHei"), size: 9pt)
#outline(title: "目 录", depth: 1)
#pagebreak()
```

### 3.4 验证方法（三级）

1. **页数与元数据**：pypdf 读页数、标题、作者；
2. **字体嵌入**：逐页枚举 `/Font` 资源（BaseFont 计数）；
3. **残留字体定性**：fonttools 比对字符覆盖 + pikepdf 解流看 `Tf` 上下文，区分正文可见字符与不可见文本层。

---

## 4. 坑位记录（本轮实测）

| # | 现象 | 根因 | 解法 |
| --- | --- | --- | --- |
| 1 | `#set table(... font: ...)` 编译报错 | typst 的 table 无 font 参数 | 改 `#show table: set text(font: ...)` |
| 2 | 脚本报 `Non-ASCII character … but no encoding declared` | Git-Bash 的 `python` = 老 Anaconda **Python 2.7.3（32 位）**，非 Python 3 | 统一用 `py -3`／`python3` 执行；脚本弃用 `u''` 前缀与编码声明 |
| 3 | heredoc 内复杂反斜杠正则反复 SyntaxError | 长 heredoc 经 shell → 解释器多层转义易损 | 把逻辑固化进 `tools/` 脚本文件执行，避免长 heredoc |
| 4 | 控制台中文显示为乱码（`ҳ��`） | 控制台 GBK 码页 vs UTF-8 输出 | 纯显示层问题；必要时 `PYTHONIOENCODING=utf-8` 或写文件后查看 |
| 5 | `glob` 的 `(a\|b)` 分组匹配不到文件 | glob 仅支持 `*?[]`，不认正则分组 | `glob('*.md')` + `re.match` 过滤 |
| 6 | `sorted()` 汇编后卷序错乱 | 字典序 ≠ 中文数字序 | 显式中文数字序列表（脚本 ORDER_CN） |
| 7 | PDF 里出现 LiSu／DejaVu／Libertinus／Emoji 等字体 | 不可见文本抽取层、等宽路径、个别西文符号与 Emoji 兜底 | 用 pikepdf/fonttools 判定非正文可见字符即可放行 |
| 8 | 方正小标宋在 `C:\Windows\Fonts` 找不到同名文件 | 字体注册位置/文件名不同 | 以 `typst fonts` 列出的 family 名（FZXiaoBiaoSong-B05S）为准即可使用 |
| 9 | 合订本头部信息与内容不同步（卷数/岗位数滞后） | 手写头部未随内容更新 | 汇编脚本统一生成；本次已勘误 |

---

## 5. 验收数据

| 产物 | 页数 | 体积 | 字体（逐页） | 其他校验 |
| --- | --- | --- | --- | --- |
| 设定集 PDF | **194** | 7.0 MB | SimSun×194、方正小标宋×191 | 目录 3 页；元数据标题/作者齐；`%%EOF` 完整 |
| 名录 PDF（分册版） | **103** | 4.68 MB | SimSun×103、方正小标宋×102 | 元数据标题齐 |
| 设定集合订本 MD | — | 585 KB | — | 11 263 行；47 部分；114 个一级标题；脚本重跑**字节级一致** |
| 名录合订本 MD | — | 342 KB | — | 6 158 行；42 卷 H1 全在；头部已勘误 |

审查结论：**真撞名 = 0**；引用链（市长↔城市卷、署长↔署卷、区主席↔城市卷）**全闭环**；品级链（六品州府／七品省府／九品区）一致。

---

## 6. 复现入口

```bash
# ① 仅汇编（幂等，可任意次重跑）
py -3 tools/build_setting_book.py

# ② 汇编 + 生成 PDF（pandoc、typst 在 PATH 时）
py -3 tools/build_setting_book.py --pdf "$USERPROFILE/Desktop/中华帝国设定集(2026).pdf"

# ③ 验收（页数/元数据；字体枚举可参考报告 §3.4 的 pypdf/fonttools/pikepdf 三段式）
uv run --with pypdf python -c "from pypdf import PdfReader; r=PdfReader(u'中华帝国设定集(2026).pdf'); print(len(r.pages), r.metadata.title)"
```

---

## 7. 待办与建议

| 项 | 说明 |
| --- | --- |
| 昭穆帝名讳 | 卷四十二唯一遗留决策点（作者拍板后同步设定集与 PDF） |
| 版面增强（可选） | 封面页、分卷页眉、奇偶页对称边距 |
| 字号档（可选） | 现为 10.5pt/9pt（紧凑 194 页）；舒适版可调 12pt/10pt |
| 全集套装（可选） | 如日后需要，可把《华北风暴》正文并入另出"全集"；当前按指示排除 |
| 字体授权提示 | 方正小标宋为商业字体；PDF 外发注意授权（内部使用无碍） |

---

报告完 · 2026-09-20 · 仓库 the-empire-of-china（main） · 汇编脚本 `tools/build_setting_book.py`
