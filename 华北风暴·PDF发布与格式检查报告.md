# 《华北风暴》正文 PDF 发布与格式检查报告

> 编制时间：2026-09-21（继文十一年）
> 产物：桌面 `华北风暴(2026).pdf` —— **299 页 / 1.85 MB / 字体全嵌入 / 正文仿宋 + 标题方正小标宋**

---

## 0. 摘要

| 项 | 结果 |
| --- | --- |
| 输出 | `%USERPROFILE%\Desktop\华北风暴(2026).pdf` |
| 页数 / 体积 | **299 页**（封面 1 + 目录 2 + 正文 296）／**1.85 MB** |
| 字体 | 正文 **仿宋 FangSong**（simfang.ttf，299 页全用）＋ 标题 **方正小标宋 FZXBSJW**（61 页），**全部子集嵌入**（共 360 处引用） |
| 版式 | A4；版心 2.2cm × 2.0cm；正文 12pt、首行缩进 2 字符、行距 19.4pt（1.62 倍）；章/部标题另起一页 |
| 检查 | `tools/check_novel_pdf.py` **35/35 项通过**（源结构 6 + 字体覆盖 2 + PDF 实物 15 + 版式几何 12） |
| 管线 | pandoc 3.10.2（gfm→typst）→ 注入封面/目录/字体版式 → typst 0.15.1 compile |

---

## 1. 源稿处理（`tools/build_novel_book.py`）

源文件 `华北风暴·正文·扩写稿.md`（7591 行正文 / 54 章 + 2 尾声 + 后记）在转 PDF 前做四步规范化：

| 步骤 | 处理 | 数量 |
| --- | --- | --- |
| 拆封面 | 抽出书名、副题「中华帝国官场长篇小说」、题头元信息（时间/地点/核心事件）→ 排成封面页 | 3 行 |
| 分隔线分类 | 标题前的结构线、文末收尾线**剔除**；**章节中部的场景分隔线保留**并渲染为居中的 `※　※　※` | 剔除 94 + 保留 2 |
| 空行压缩 | 连续空行压成 1 个段落分隔 | — |
| 引号归一 | 半角 `"` → `“”`（983 对）、半角 `'` → `‘’`（66 对，中文内层引号规范） | 1966 + 132 个字符 |

> **为什么必须做引号归一**：源稿混用中文引号（1349 对）与半角引号（983 对）。半角引号经 pandoc 转 typst 时被转义成 `\"`，typst 的弯引号替换不再生效，直接排出来是西文直引号——PDF 里会明显"不像中文书"。源稿每行引号数均为偶数（无跨段引号），逐行配对无歧义。
> 如需还原：`--no-quote-fix`。

---

## 2. 版式规范（typst 注入）

| 元素 | 设置 |
| --- | --- |
| 正文 | 仿宋 12pt，`justify` 两端对齐，首行缩进 **2em = 24pt**，行距 **19.4pt**，段间距 = 行距 |
| 章标题（H2） | 方正小标宋 20pt，居中，**另起一页**（`pagebreak(weak: true)`） |
| 部标题（H1） | 方正小标宋 26pt，居中，另起一页 + 顶部留白 3.4cm |
| 目录标题 | 方正小标宋 24pt，居中；目录 depth 2（部 + 章），点线页码 |
| 引用块（题记/附记） | 仿宋 11pt，左缩进，首行不缩进 |
| 页码 | 正文页脚居中，**从 1 起**；封面、目录不编页码 |
| 页面 | A4，边距 x 2.2cm / y 2.0cm，`lang: "zh"`（中文断行与标点压缩） |

---

## 3. 格式检查（`tools/check_novel_pdf.py`，35 项全通过）

**A 源结构（6）**：章号 1–54 连续无重复；57 个 H2（54 章 + 尾声×2 + 后记）齐全；部标题 3 条；分隔线分类正确（97 条 = 标题前 93 + 场景分隔 2 + 文末 2）。

**B 字体覆盖（2）**：按 fonttools 直接读 cmap 比对——正文用字 **2447 个不同字符，仿宋缺 0**；标题用字 180 个，方正小标宋缺 0。故不存在"豆腐块"或静默回退。

**C PDF 实物（15）**：页数/元数据（title=华北风暴，author=Q1y1ng）；逐页枚举 `/Font` 资源，**字体全部嵌入**；无空白页；57 个章标题全部可在文本层检索且**各自独立起页**；**目录页码 57/57 与实际页脚一一对应**（逐条交叉核对，错位 0）；场景分隔符 `※※※` 渲染 2 处；罕见字 窸/窣/镚 未丢失；**半角直引号 0 个**、中文引号 4664 个（另有内层单引号 ‘ ’ 299 个）；`%%EOF` 完整。

**D 版式几何（12）**：A4 595.3×841.9pt；版心左 62.4pt（=2.2cm）、右留白 56.4pt（差 6.0pt 为中文标点悬挂，正常）；正文 12pt / 仿宋；**首行缩进实测 24.00pt（恰 2 字符）**；**行距实测 19.41pt**，段间 19.42pt（与行距一致，无额外段距）；章标题 20pt / 部标题 26pt / 目录标题 24pt 均为方正小标宋且居中；页码居中，封面目录无页码。

> 复核命令：`py -3 tools/check_novel_pdf.py`（依赖缺失时脚本自动用 uv 建临时环境重跑）

---

## 4. 源稿内容问题清单（**仅记录，未改动源文件**）

| # | 位置 | 现象 | 建议 |
| --- | --- | --- | --- |
| 1 | 正文第 1 行 | **缺「第一部」部标题**：文件直接从 `## 第一章` 开始，而第二部/第三部都有 `# 第X部` 标题 | 补 `# 第一部 华北案（1990-1991）` |
| 2 | 第 6526 行起 | `# 第三部 焊住的盆（2001-2010）` 之下实际含第四十三章（2001）至第五十四章（2026），**缺「第四部 盆的成年」标题**；题头却声明"全书四部" | 在第四十三章前补 `# 第四部 盆的成年（2010-2026）` |
| 3 | 第 6528 行 | `> 第三、四部已按2026定版大纲直编，余人九年仅晋半级或一级……` —— 这是一条**作者附记（编辑说明）**，会印进正文 | 若属自用备注，建议移出正文（或改为脚注） |
| 4 | 第 3920 / 5412 行 | 两处「尾声 盆还在」（后者为「盆还在·续」），且第四十二章另有「终章·盆永在」 | 如需保留"每部一尾声"结构，建议统一命名 |
| 5 | 第 3454 / 3142 行 | 罕见字 窸、窣、镚（仿宋**有**这些字，方正小标宋**没有**——本次二者均不在标题中，故无影响） | 若将来标题要用这三个字，需换标题字体或加回退 |
| 6 | 全文 97 条 `---` | 93 条是标题前的结构线、2 条是章节中部的场景分隔、2 条是文末收尾；其中 **37 处成对重复**（`---` 空行 `---`） | 冗余结构线可清理（PDF 端已按类型正确处理） |

---

## 5. 坑位记录（本轮实测）

| # | 现象 | 根因 | 解法 |
| --- | --- | --- | --- |
| 1 | 封面后多出一整张空白页，目录被推到第 3 页 | `#outline(title: [目　录])` 的标题是**1 级 heading**，被「部标题」show 规则接管 → `pagebreak(weak: false)` 再断一次页 | 目录标题自绘（`text(...)`），`#outline(title: none, depth: 2)` |
| 2 | 行距怎么调都偏小（16.18pt），且**段间距比行距还紧** | typst 行高 = **字体固有行高（本机仿宋 @12pt ≈ 8.0pt = 0.667em）+ `par(leading)`**；而**段间距用 `par(spacing)` 顶替 `leading`**，不是叠加 | `leading` 与 `spacing` 取同一值 0.95em ⇒ 全篇 19.4pt 均一 |
| 3 | 半角引号在 PDF 里仍是西文直引号 | pandoc 转义为 `\"`，typst 弯引号替换失效 | 转换前做引号归一（见 §1） |
| 4 | 中文里出现 `#horizontalrule` / `#divider` 命名困惑 | typst 无 `horizontalrule` 元素；pandoc 对 gfm `---` 输出的是 `#divider()`（0.15.1 可用） | 场景分隔线用 `#show divider` 样式化，结构线直接剔除 |
| 5 | 用 PIL 数"文本行"数不准（28 行 vs 实际 40+ 行） | 相邻行墨迹间隙 ≤4px 被误并为一行 | 行距改用 pymupdf **基线聚簇**（origin y）判定，墨迹法只作旁证 |
| 6 | pymupdf 的 `line` 分组在中文排版下会拆行/并跨行 | 一个 line 内可含多段 span、跨行合并 | 自写 `baseline_rows()` 按 `origin[1]` 聚簇还原视觉行 |
| 7 | `uv run --with pymupdf` 卡在 18.9MB 下载 | 默认 PyPI 源慢 | 加 `--index-url https://pypi.tuna.tsinghua.edu.cn/simple`（脚本内已固化镜像） |

---

## 6. 复现入口

```bash
# ① 生成 PDF（默认输出到桌面）
py -3 tools/build_novel_book.py
py -3 tools/build_novel_book.py --pdf "$USERPROFILE/Desktop/华北风暴(2026).pdf"
py -3 tools/build_novel_book.py --keep-tmp        # 保留中间 .typ 便于排查

# ② 格式检查（35 项；依赖缺失时自动用 uv 建临时环境）
py -3 tools/check_novel_pdf.py

# ③ 只看某几页的渲染（预览/校对）
typst compile --format png --ppi 110 --pages 4,154,299 "$TEMP/novel_book.tmp.typ" "$TEMP/p{n}.png"
```

---

## 7. 待作者定夺

| 项 | 说明 |
| --- | --- |
| 第一/第四部标题 | 见 §4-1、§4-2，补标题后需重跑 ①②（约 1 分钟） |
| 作者附记是否入书 | 见 §4-3 |
| 源稿引号是否一并归一化 | 本次只在 PDF 端归一；若同意，可对 `华北风暴·正文·扩写稿.md` 做同样处理并提交 |
| 场景分隔样式 | 现用居中 `※　※　※`；可改短横线、空行留白或星号 |
